# Codespaces preview recovery

For an existing Codespace, rebuild the container after `.devcontainer` configuration changes. New Codespaces use the configuration automatically. Port 8080 is the browser preview endpoint.