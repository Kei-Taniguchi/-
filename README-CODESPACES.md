# Codespaces browser preview

- Node.js 22
- `npm install` at container creation
- `bash .devcontainer/start-server.sh` at container start
- HTTP server on `0.0.0.0:8080`
- automatic forwarding of 8080
- automatic browser open

For an existing Codespace, rebuild the container after `.devcontainer` changes.