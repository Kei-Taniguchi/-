# GitHub Codespaces プレビュー

新規CodespaceではNode.js 22、依存関係、Webサーバー、8080番ポートの転送を自動設定します。

既存Codespaceで設定変更が反映されない場合は、Command Paletteから **Codespaces: Rebuild Container** を実行してください。

ポート一覧に `8080 / 家計簿 Webアプリ` が表示されたら **Open in Browser** で確認できます。
