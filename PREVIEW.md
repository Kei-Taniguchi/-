# Codespaces preview

New Codespaces automatically install Node.js dependencies, start the HTTP server on port 8080, forward the port, and open the browser.

If an existing Codespace does not pick up `.devcontainer` changes, run **Codespaces: Rebuild Container** once. Then open port 8080 from the Ports panel using **Open in Browser**.
